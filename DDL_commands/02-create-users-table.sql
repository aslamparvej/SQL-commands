-- For creating table:- CREATE TABLE TABLE_NAME(columns and their datatype);

CREATE TABLE users(
    full_name VARCHAR(200),
    yearly_salary INT,
    current_status ENUM('employed','self_employed','unemployed')
);
